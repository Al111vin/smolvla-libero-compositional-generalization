# SmolVLA-LIBERO 项目工作规则

## Gate 5 基准保护

- 原始 LIBERO-36 严格 Gate 5 结果保持为 32/36。
- 任务 15、17、33、35 记录为物理不可行；不得把它们改写成通过。
- 不得覆盖或修改 `results/libero36_gate5_official_v6/`。
- 不得删除或覆盖 `results/libero36_gate5_physical_infeasible/`，其中的失败视频、轨迹、日志和原因必须保留。
- 不得放宽碰撞判定、抬升阈值、动作上限或其他 Gate 5 条件。
- 任何替换任务必须同时满足相同的 `object × skill × spatial` 组合和严格 Gate 5；否则只能作为诊断结果，不能计入正式结果。

## 数据采集与训练

- 训练数据只使用 32 个可行任务；物理不可行任务不进入正式训练集，除非另有明确标注的对照实验。
- 新实验使用独立输出目录，不覆盖 Gate 5 证据或既有训练结果。
- 每次实验记录数据版本、代码版本、随机种子、GPU/软件环境、训练配置和评估结果。
- 采集数据必须检查：文件可读、帧数一致、图像尺寸、状态/动作维度、NaN/Inf、动作范围和 episode 完整性。
- 随机 smoke 数据只能验证流程，不能直接作为专家训练数据。

## 结果报告

- 明确区分“原始严格 Gate 5 结果”“物理不可行归档”和“诊断/替换实验”。
- 不得将替换基准或诊断通过写成原始 LIBERO-36 36/36 正式通过。
- 修改代码或生成新结果前，先检查工作区现有改动和相关 manifest，避免污染用户已有成果。
